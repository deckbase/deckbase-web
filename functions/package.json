{
  "name": "deckbase-functions",
  "description": "Firebase Cloud Functions (e.g. trigger speech analyzer on new transcript)",
  "main": "index.js",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
