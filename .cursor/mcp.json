{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "experimental:mcp",
        "--dir",
        "${workspaceFolder}",
        "--only",
        "core,firestore,storage,auth"
      ]
    }
  }
}